{
  "name": "latentspacelabs-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Latent Space Labs monorepo. Docs under docs/, owned TypeScript packages under packages/.",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  }
}
